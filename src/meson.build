e = dependency('enlightenment')

module = shared_module('module',
                       'e_mod_main.c',
                       dependencies : [e, elm],
                       install_dir: install_dir,
                       install: true,
                       name_prefix: '')
